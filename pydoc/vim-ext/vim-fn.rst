Vimscript Functions
===================

Venom provides access to useful vimscript functions in python through the ``vim.fn`` add-in interface. Some
functions are specific to vimscript (e.g. vimscript data structure manipulation functions) and are not made available.

